Goal: <%= @goal.name %>
<br>
<% @insight.answers.each do |answer| %>
  <%= answer.content %>
  <%= simple_form_for([@goal, @insight, @comment]) do |f| %>
    <%= f.input :content %>
    <%= f.button :submit %>
  <% end %>
  <br>
<% end  %>
